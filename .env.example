POSTGRES_URL=                # Vercel Postgres / Neon 接続文字列
GITHUB_TOKEN=                 # Fine-grained PAT, public_repo read
ANTHROPIC_API_KEY=
CRON_SECRET=                  # /api/cron/* 保護用（自分で生成した任意の文字列）

# ossalt-next（別プロジェクト）の import_candidates への自動登録用
OSSALT_SUPABASE_URL=https://acakchddmmuylifrgkbv.supabase.co
OSSALT_SUPABASE_SERVICE_ROLE_KEY=   # ossalt-nextのSupabase Dashboard → Settings → API → service_role
SITE_URL=                            # 省略時は https://repo-summary-ja.vercel.app
